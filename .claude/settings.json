{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in */app/*.ts|*/app/*.tsx) cd \"${CLAUDE_PROJECT_DIR:-.}/app\" && npx --no-install tsc --noEmit;; esac; }",
            "timeout": 90,
            "statusMessage": "Type-checking app/..."
          }
        ]
      }
    ]
  }
}
