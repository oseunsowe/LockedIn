import { ModalStub } from '@/components/ModalStub';

export default function ProofModal() {
  return (
    <ModalStub
      icon="camera"
      title="Submit Proof"
      phase="PHASE 8 — NOT BUILT"
      description="Camera, screenshot, voice, and file proof options."
    />
  );
}
