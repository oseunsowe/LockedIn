import { ModalStub } from '@/components/ModalStub';

export default function VerificationModal() {
  return (
    <ModalStub
      icon="verified"
      title="AI Verification"
      phase="PHASE 8 — NOT BUILT"
      description="Confidence score, XP reward, and achievement unlock — server-proxied Claude call, never a client-side key."
    />
  );
}
