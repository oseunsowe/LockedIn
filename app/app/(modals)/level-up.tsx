import { ModalStub } from '@/components/ModalStub';

export default function LevelUpModal() {
  return (
    <ModalStub
      icon="xp"
      title="Level Up"
      phase="PHASE 9 — NOT BUILT"
      description="The full-screen celebration moment — give it real animation budget when built."
    />
  );
}
