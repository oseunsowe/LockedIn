import { StubScreen } from '@/components/StubScreen';

export default function ProgressStub() {
  return (
    <StubScreen
      icon="progress"
      title="Progress Profile"
      phase="PHASE 11 — NOT BUILT"
      description="Level, class, achievements grid, mission history, execution score trends."
    />
  );
}
