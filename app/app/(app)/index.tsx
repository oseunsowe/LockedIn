import { StubScreen } from '@/components/StubScreen';

export default function DashboardStub() {
  return (
    <StubScreen
      icon="home"
      title="Dashboard"
      phase="PHASE 6 — NOT BUILT"
      description="Execution Score ring + one dominant Main Quest card. This route exists to prove the tab bar and navigation work."
    />
  );
}
