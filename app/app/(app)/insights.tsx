import { StubScreen } from '@/components/StubScreen';

export default function InsightsStub() {
  return (
    <StubScreen
      icon="ai"
      title="AI Insights"
      phase="PHASE 6 — NOT BUILT"
      description="Read-only AI insight feed — the MVP stand-in for the cut voice-first AI Coach screen."
    />
  );
}
